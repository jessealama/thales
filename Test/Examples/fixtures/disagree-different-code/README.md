# disagree-different-code (placeholder)

Intended to catch cases where tsc and thales both reject the same position but
with different TS#### codes. No such case exists today. `expected-outcome.txt`
is temporarily `pass:accept`; update when a real mismatch surfaces.
