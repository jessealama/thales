# disagree-different-column (placeholder)

Slot for cases where tsc and thales reject with the same (line, code) but
different columns. Currently `pass:accept`; replace when a real mismatch
surfaces.
