# disagree-different-line (placeholder)

Intended to catch cases where tsc and thales reject with the same TS#### code
but on different lines. Currently `pass:accept`; replace when a real mismatch
surfaces.
